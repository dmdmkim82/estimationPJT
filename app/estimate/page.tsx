import { EstimateStudio } from "@/components/estimate-studio";

export default function EstimatePage() {
  return (
    <main className="page-shell page-shell--studio page-shell--estimate">
      <EstimateStudio />
    </main>
  );
}
